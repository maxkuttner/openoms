{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Grants the bundled local connection page access to the `connect` command. There is no `remote` block, so this capability is never extended to any server address the window later navigates to — Tauri resolves permissions per current-page origin (local vs. remote), not by window label, and the `main` window carries no permissions at all once it is showing remote content.",
  "windows": ["main"],
  "permissions": [
    "allow-connect"
  ]
}
